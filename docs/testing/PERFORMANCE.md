# Benchmark Results (Placeholder)

Empirical performance measurements for IBVAP Internal-Round MVP.

> [!NOTE]
> Benchmark results will be recorded here following physical execution during Phase 10 (Testing & Benchmarking).

## Measured Metrics Template
- **Hardware Profile**: (e.g. Apple M-series / Intel i7 / GPU)
- **Detection FPS**: Target > 15 FPS local execution
- **Inference Latency**: Target < 50ms per frame
- **Tracking ID Switches**: Count across test dataset
- **Detection-to-Alert Latency**: Target < 200ms
- **SHA-256 Hash Computation Time**: Target < 10ms per snapshot
